#include <stdio.h>

int main() {
   
  int num = 10; // Inicializa a variável

  while (num >= 1) { // Verifica a condição no início
      printf("%i,", num); // Imprime o número atual
      num--; // Decrementa num
  }

  return 0; 
}
